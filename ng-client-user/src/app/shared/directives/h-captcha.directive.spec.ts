import { HCaptchaDirective } from './h-captcha.directive';

describe('HCaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new HCaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
